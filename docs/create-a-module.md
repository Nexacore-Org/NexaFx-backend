## Pre-Scaffolding Checklist
Before creating a new module via CLI or manual scaffolding:
1. Search both `src/` and `src/modules/` to verify whether a module with the same name already exists.
2. If an implementation exists, extend or call into it rather than creating a duplicate stub.